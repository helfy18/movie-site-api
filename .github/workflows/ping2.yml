name: Ping Movies API2

on:
  schedule:
  - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
    - name: Send request to Movies API
      run: curl -X GET https://rival-rodie-helfy18-8da32b7b.koyeb.app/movies/count
